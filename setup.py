# -*- coding: utf-8 -*-

from setuptools import setup, find_packages

setup(
    name="console_menu",
    version="0.1.1",
    packages=find_packages(),
    install_requires=[],  # additional requires
)
